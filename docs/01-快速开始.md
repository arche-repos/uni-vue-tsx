# 快速开始

本模板基于 uni-app + Vue3 + TypeScript，开箱即用，支持微信小程序和 H5 端。

## 📦 使用模板

1. **GitHub 模板使用**
   - 点击 GitHub 仓库页面的 "Use this template" 按钮
   - 填写你的新仓库名称和描述
   - 选择是否包含所有分支

2. **克隆使用**
   ```bash
   git clone https://github.com/arche-repos/uni-vue-tsx.git your-project-name
   cd your-project-name
   ```

## 🚀 环境准备

**必须安装的 VS Code 扩展：**
- ESLint - 代码质量检查
- Prettier - 代码格式化  
- Vue Language Features (Volar) - Vue3 支持

## 📋 开发流程

### 1. 安装依赖
```bash
yarn install
```

### 2. 开始开发
```bash
# 微信小程序开发
yarn dev:mp-weixin

# H5 开发
yarn dev:h5
```

### 3. 代码检查
```bash
# ESLint 检查
yarn lint

# TypeScript 类型检查
yarn type-check
```

### 4. 构建发布
```bash
# 微信小程序
yarn build:mp-weixin

# H5 端
yarn build:h5
```

## 🎯 多平台发布指南

| 平台 | 开发命令 | 构建命令 | 预览方式 |
|------|----------|----------|----------|
| 微信小程序 | `yarn dev:mp-weixin` | `yarn build:mp-weixin` | 微信开发者工具打开 `dist/build/mp-weixin/` |
| H5 | `yarn dev:h5` | `yarn build:h5` | 浏览器访问本地服务 |
| 支付宝小程序 | `yarn dev:mp-alipay` | `yarn build:mp-alipay` | 支付宝小程序开发者工具 |
| 抖音小程序 | `yarn dev:mp-toutiao` | `yarn build:mp-toutiao` | 抖音小程序开发者工具 |

## 🔧 开发提示

- **自动格式化**：保存文件时自动格式化代码
- **自动整理**：保存时自动整理 import 语句
- **跨编辑器**：支持 VS Code、WebStorm 等多编辑器
- **类型安全**：完整的 TypeScript 支持